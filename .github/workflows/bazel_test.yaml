name: Run tests
on: [push]
jobs:
  run-bazel-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: bazel test //...
    # runs-on: gcr.io/bazel-public/bazel:7.3.2
    # steps:
    #   - run: test ...
